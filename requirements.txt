# Runtime requirements for the HCAI group project (all four apps).
# Verified on a clean venv. Bump only by agreement.

Django>=5.0,<6.0

numpy>=1.26,<3
pandas>=2.2,<3
matplotlib>=3.8,<4
scikit-learn>=1.4
joblib>=1.3
